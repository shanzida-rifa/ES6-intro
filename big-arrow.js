const add = (n1, n2) => n1 + n2;
const sum = add(445, 98);

const getname = () => 'rifa shanzida khanam';

const doMath = (x, y) => {
    const sum = x + y;
    const diff = x - y;
    const result = sum * diff;
    const output = result * 5;
    return output;
}

console.log(doMath(4, 5));

document.getElementById('my-btn').addEventListener(event => {

})
